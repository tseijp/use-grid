---
sidebar_position: 2
sidebar_label: Media
---

# Media

import {Media} from 'use-grid/src'
import {Live} from '../components'

<Live.Card scope={{React, Media}} code={`
render (
  <Media
    minWidth="576px"
    maxWidth="768px"
    init={false}
  >
    {(medium, ref) =>
      <div ref={ref}>{medium? "👧": "🧔"}</div>
    }
  </Media>
)
`.trim()}/>
