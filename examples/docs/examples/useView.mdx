---
sidebar_position: 6
sidebar_label: useView
---

# useView

import {useView} from 'use-grid/src';
import {Live} from '../components'

<Live.Card scope={{React, useView}} noInline={false} code={`
function App () {
  const ref = React.useRef()
  const view = useView(ref)
  return (
    <div ref={ref}>
      {view? "🥱": "😴"}
    </div>
  )
}`.trim()}/>
