---
sidebar_position: 3
sidebar_label: View
---

# View

import {View} from 'use-grid'
import {Live} from '../../components'

<Live.Card scope={{React, View}} code={`
render (
  <View>
    {(view, ref) =>
      <div ref={ref}>{view}</div>
    }
  </View>
)
`.trim()}/>
