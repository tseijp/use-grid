---
sidebar_position: 4
sidebar_label: Utilities
---

import {Live} from '../../components/Live'
import {
    convertNumToPix,
    convertPropsToList,
    covertPrefixToList,
    convertFuncionToList
} from 'use-grid/src'

## Grid Utils

- `mergeConfig`


- `covertPrefixToList`

<Live.Card scope={{React, convertPropsToList}} code={`
render (convertPropsToList + "")
`.trim()}/>


- `convertPropsToList`


<Live.Card scope={{React, convertPropsToList}} code={`
render (convertPropsToList + "")
`.trim()}/>


- `convertFuncionToList`


<Live.Card scope={{React, convertFuncionToList}} code={`
render (convertFuncionToList + "")
`.trim()}/>

## Media Utils

- `defaultMedia`: `MediaString`
- `defaultConfig`: `Config`
- `convertNumToPix`

<Live.Card scope={{React, convertNumToPix}} code={`
render (convertNumToPix + "")
`.trim()}/>


## Helpers

- `is`
